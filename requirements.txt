# Mirrors [project.dependencies] in pyproject.toml, which is the source of
# truth. The two pins below are transitive, not direct: Snyk flags them on
# this file, so they stay until the upstream floors move.
pyserial>=3.5
requests>=2.20
tqdm>=4.60
click>=8.1
rich>=14.0
packaging>=21.0
urllib3>=2.6.3 # not directly required, pinned by Snyk to avoid a vulnerability
idna>=3.15 # not directly required, pinned by Snyk to avoid a vulnerability
